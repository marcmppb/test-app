import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        flexDirection: 'row',
        alignItems:'center',
        gap:5,
  
    },
    textInput: {
        borderWidth: 0.5,
        borderColor:'grey',
        padding:3,
        backgroundColor: '#fff',
    }
})
export default styles;